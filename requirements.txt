# GazeKey product runtime (Python 3.11; discovery 3.11.9).
# Do not install or run the product on the previous Python 3.14 venv.
PySide6>=6.6.0
opencv-python>=4.8.0
numpy>=1.24.0
pynput>=1.7.6
pygame>=2.5.0
pandas>=2.0.0
screeninfo>=0.8.1
MNN>=2.0.0

# Official GazeFollower v1.0.2 at commit 553920edcb7998c029828677f50f6d8eb4a16249.
# Upstream setup.py imports GazeFollower at build time, so install with:
#   python -m pip install --no-build-isolation -r requirements.txt
# Adapter code must `import gazefollower` and MUST NOT vendor upstream into gazekey/.
# GazeFollower still uses mediapipe internally; do not add a GazeKey-owned
# face_landmarker.task / MediaPipe Face Landmarker tracking pin.
gazefollower @ git+https://github.com/GanchengZhu/GazeFollower.git@553920edcb7998c029828677f50f6d8eb4a16249
